# MAIN FILES OF FRONTEND

`feedback-platform.tsx` - This is the main component that renders the entire feedback platform UI
`animated-score-card.tsx` - A reusable component for displaying score cards with animations
`grade-legend.tsx` - Component that displays the grade scale with visual elements
`app/page.tsx` - The main page component that imports and renders the FeedbackPlatform
`app/layout.tsx` - The root layout component for the Next.js app
`tailwind.config.js` - Configuration for Tailwind CSS

To run the project:
-  Install dependencies: 'npm install or yarn install', then 'npm run build or yarn build', afterwards 'npm start  or yarn start '.

## Technologies Used

- **Next.js**: React framework for server-rendered applications
- **Tailwind CSS**: Utility-first CSS framework
- **Framer Motion**: Animation library for React
- **Recharts**: Composable charting library for React
- **Lucide React**: Icon library


The application will be available at [http://localhost:3000]

